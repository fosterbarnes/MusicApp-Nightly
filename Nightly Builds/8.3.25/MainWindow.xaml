<Window x:Class="MusicApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MusicApp"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Music App" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E"
        WindowStyle="None"
        AllowsTransparency="False"
        ResizeMode="CanResize">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Dark" PrimaryColor="Purple" SecondaryColor="Lime" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
            
            <!-- Converter to calculate slider fill width -->
            <local:SliderValueToWidthConverter x:Key="SliderValueToWidthConverter"/>
            <local:SliderValueToWidthMultiConverter x:Key="SliderValueToWidthMultiConverter"/>
            
            <!-- Custom Button Style for Media Controls -->
            <Style x:Key="CustomMediaButton" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Width" Value="48"/>
                <Setter Property="Height" Value="48"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="24"
                                    Width="{TemplateBinding Width}"
                                    Height="{TemplateBinding Height}">
                                <Grid>
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Panel.ZIndex="2"/>
                                    <Ellipse x:Name="Ripple" Fill="#705399" Opacity="0" Width="48" Height="48" 
                                            HorizontalAlignment="Center" VerticalAlignment="Center" Panel.ZIndex="1">
                                        <Ellipse.RenderTransform>
                                            <ScaleTransform x:Name="RippleScale" ScaleX="0" ScaleY="0" CenterX="24" CenterY="24"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <EventTrigger RoutedEvent="Button.PreviewMouseDown">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Ripple" 
                                                             Storyboard.TargetProperty="Opacity" 
                                                             To="0.3" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="RippleScale" 
                                                             Storyboard.TargetProperty="ScaleX" 
                                                             To="1" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="RippleScale" 
                                                             Storyboard.TargetProperty="ScaleY" 
                                                             To="1" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="Ripple" 
                                                             Storyboard.TargetProperty="Opacity" 
                                                             To="0" Duration="0:0:0.2" BeginTime="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="RippleScale" 
                                                             Storyboard.TargetProperty="ScaleX" 
                                                             To="0" Duration="0:0:0.1" BeginTime="0:0:0.4"/>
                                            <DoubleAnimation Storyboard.TargetName="RippleScale" 
                                                             Storyboard.TargetProperty="ScaleY" 
                                                             To="0" Duration="0:0:0.1" BeginTime="0:0:0.4"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2D2D2D"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            
            <!-- Custom Volume Slider Style -->
            <Style x:Key="CustomVolumeSlider" TargetType="Slider">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Slider">
                            <Grid>
                                <!-- Background Track -->
                                <Border Height="4" Background="#404040" VerticalAlignment="Center" CornerRadius="2"/>
                                
                                <!-- Filled Portion -->
                                <Border Height="4" Background="#705399" VerticalAlignment="Center" CornerRadius="2" 
                                        HorizontalAlignment="Left">
                                    <Border.Width>
                                        <MultiBinding Converter="{StaticResource SliderValueToWidthMultiConverter}">
                                            <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="Value"/>
                                            <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="Maximum"/>
                                            <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="ActualWidth"/>
                                        </MultiBinding>
                                    </Border.Width>
                                </Border>
                                
                                <!-- Slider Track for Interaction -->
                                <Track x:Name="PART_Track">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="Slider.DecreaseLarge" Opacity="0" Focusable="false"/>
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="Slider.IncreaseLarge" Opacity="0" Focusable="false"/>
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb x:Name="Thumb" Background="#705399">
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="Thumb">
                                                    <Ellipse Fill="#705399" Width="20" Height="20"/>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                        </Thumb>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border BorderBrush="#404040" BorderThickness="1" Background="#1E1E1E">
        <Grid Background="#1E1E1E">
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

        <!-- Combined Title Bar and Player Controls -->
        <Border Grid.Row="0" Background="#1E1E1E" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" VerticalAlignment="Top">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Playback Controls (Left) -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,10,0,10">
                    <Button x:Name="btnPrevious" Style="{StaticResource CustomMediaButton}" 
                            Foreground="White" Margin="0,0" Click="BtnPrevious_Click">
                        <materialDesign:PackIcon Kind="SkipPrevious" Width="24" Height="24"/>
                    </Button>
                    
                    <Button x:Name="btnPlayPause" Style="{StaticResource CustomMediaButton}" 
                            Foreground="White"  Margin="2,0" Click="BtnPlayPause_Click">
                        <materialDesign:PackIcon x:Name="iconPlayPause" Kind="Play" Width="24" Height="24"/>
                    </Button>
                    
                    <Button x:Name="btnNext" Style="{StaticResource CustomMediaButton}" 
                            Foreground="White" Margin="0,0" Click="BtnNext_Click">
                        <materialDesign:PackIcon Kind="SkipNext" Width="24" Height="24"/>
                    </Button>
                </StackPanel>

                <!-- Volume Controls (Center-Left) -->
                                  <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="30,10,0,10">
                      <Slider x:Name="sliderVolume" Width="100" Minimum="0" Maximum="100" Value="100" 
                              VerticalAlignment="Center" ValueChanged="SliderVolume_ValueChanged"
                              Style="{StaticResource CustomVolumeSlider}"
                              Foreground="#705399" Background="Transparent"/>
                      <Border Background="Transparent" Padding="8" VerticalAlignment="Center" Margin="5,0,0,0" 
                              MouseLeftButtonDown="IconVolume_MouseLeftButtonDown">
                          <materialDesign:PackIcon x:Name="iconVolume" Kind="VolumeHigh" Foreground="White"/>
                      </Border>
                  </StackPanel>

                <!-- Current Song Info (Center-Right) -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="30,10,0,10">
                    <!-- Album Art -->
                    <Border Width="50" Height="50" Background="#404040" CornerRadius="5" Margin="0,0,15,0">
                        <Border.Clip>
                            <RectangleGeometry Rect="0,0,50,50" RadiusX="5" RadiusY="5"/>
                        </Border.Clip>
                        <Image x:Name="imgAlbumArt" Stretch="UniformToFill"/>
                    </Border>
                    
                    <!-- Song Info -->
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="txtCurrentTrack" Text="No track selected" 
                                   Foreground="White" FontWeight="Bold" FontSize="14"/>
                        <TextBlock x:Name="txtCurrentArtist" Text="" 
                                   Foreground="#CCCCCC" FontSize="12"/>
                    </StackPanel>
                </StackPanel>

                <!-- Window Controls (Right) -->
                <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <Button x:Name="btnMinimize" Width="36" Height="30" Background="Transparent" BorderThickness="0" Cursor="Arrow"
                            Foreground="White" Click="BtnMinimize_Click" MouseEnter="WindowControl_MouseEnter" MouseLeave="WindowControl_MouseLeave">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <materialDesign:PackIcon Kind="WindowMinimize" Width="16" Height="16"/>
                    </Button>
                    <Button x:Name="btnMaximize" Width="36" Height="30" Background="Transparent" BorderThickness="0" Cursor="Arrow"
                            Foreground="White" Click="BtnMaximize_Click" MouseEnter="WindowControl_MouseEnter" MouseLeave="WindowControl_MouseLeave">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <materialDesign:PackIcon x:Name="iconMaximize" Kind="WindowMaximize" Width="16" Height="16"/>
                    </Button>
                    <Button x:Name="btnClose" Width="36" Height="30" Background="Transparent" BorderThickness="0" Cursor="Arrow"
                            Foreground="White" Click="BtnClose_Click" MouseEnter="CloseButton_MouseEnter" MouseLeave="CloseButton_MouseLeave">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <materialDesign:PackIcon Kind="Close" Width="16" Height="16"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar Navigation -->
            <Border Grid.Column="0" Background="#2D2D2D" BorderBrush="#404040" BorderThickness="0,0,1,0">
                <StackPanel Margin="10">
                    <TextBlock Text="Music App" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>
                    
                                                                                                                                                                       <Button x:Name="btnLibrary" Style="{StaticResource MaterialDesignFlatButton}" 
                               Foreground="White" HorizontalAlignment="Stretch" Margin="0,5"
                               Click="BtnLibrary_Click">
                           <Button.Template>
                               <ControlTemplate TargetType="Button">
                                   <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                       <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0"/>
                                   </Border>
                               </ControlTemplate>
                           </Button.Template>
                           <StackPanel Orientation="Horizontal">
                               <materialDesign:PackIcon Kind="Music" VerticalAlignment="Center" Margin="0,0,10,0"/>
                               <TextBlock Text="Music Library" VerticalAlignment="Center"/>
                           </StackPanel>
                       </Button>
                       
                       <Button x:Name="btnPlaylists" Style="{StaticResource MaterialDesignFlatButton}" 
                               Foreground="White" HorizontalAlignment="Stretch" Margin="0,5"
                               Click="BtnPlaylists_Click">
                           <Button.Template>
                               <ControlTemplate TargetType="Button">
                                   <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                       <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0"/>
                                   </Border>
                               </ControlTemplate>
                           </Button.Template>
                           <StackPanel Orientation="Horizontal">
                               <materialDesign:PackIcon Kind="PlaylistMusic" VerticalAlignment="Center" Margin="0,0,10,0"/>
                               <TextBlock Text="Playlists" VerticalAlignment="Center"/>
                           </StackPanel>
                       </Button>
                       
                       <Button x:Name="btnRecentlyPlayed" Style="{StaticResource MaterialDesignFlatButton}" 
                               Foreground="White" HorizontalAlignment="Stretch" Margin="0,5"
                               Click="BtnRecentlyPlayed_Click">
                           <Button.Template>
                               <ControlTemplate TargetType="Button">
                                   <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                       <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0"/>
                                   </Border>
                               </ControlTemplate>
                           </Button.Template>
                           <StackPanel Orientation="Horizontal">
                               <materialDesign:PackIcon Kind="History" VerticalAlignment="Center" Margin="0,0,10,0"/>
                               <TextBlock Text="Recently Played" VerticalAlignment="Center"/>
                           </StackPanel>
                       </Button>
                    
                    <Separator Margin="0,20" Background="#404040"/>
                    
                                                                                                                                                                       <Button x:Name="btnAddFolder" Style="{StaticResource MaterialDesignFlatButton}" 
                               Foreground="White" HorizontalAlignment="Stretch" Margin="0,5"
                               Click="BtnAddFolder_Click">
                           <Button.Template>
                               <ControlTemplate TargetType="Button">
                                   <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                       <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0"/>
                                   </Border>
                               </ControlTemplate>
                           </Button.Template>
                           <StackPanel Orientation="Horizontal">
                               <materialDesign:PackIcon Kind="FolderPlus" VerticalAlignment="Center" Margin="0,0,10,0"/>
                               <TextBlock Text="Add Music Folder" VerticalAlignment="Center"/>
                           </StackPanel>
                       </Button>
                </StackPanel>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Column="1" Background="#1E1E1E">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" Background="#2D2D2D" Padding="20,15">
                    <TextBlock x:Name="txtHeader" Text="Music Library" FontSize="24" FontWeight="Bold" Foreground="White"/>
                </Border>

                <!-- Content Area -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <Grid x:Name="mainContent" Margin="20">
                        <!-- Music Library View -->
                        <Grid x:Name="libraryView" Visibility="Visible">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Search Bar -->
                            <Border Grid.Row="0" Background="#2D2D2D" CornerRadius="5" Padding="15,10" Margin="0,0,0,20">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="txtSearch" Grid.Column="0" 
                                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                             materialDesign:HintAssist.Hint="Search music..."
                                             Foreground="White" Background="Transparent"
                                             TextChanged="TxtSearch_TextChanged"/>
                                    <Button Grid.Column="1" Style="{StaticResource MaterialDesignIconButton}"
                                            Foreground="White" Margin="10,0,0,0">
                                        <materialDesign:PackIcon Kind="Magnify"/>
                                    </Button>
                                </Grid>
                            </Border>

                            <!-- Music List -->
                            <ListView x:Name="lstMusic" Grid.Row="1" Background="Transparent" 
                                      BorderThickness="0" Foreground="White"
                                      SelectionChanged="LstMusic_SelectionChanged">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Title" Width="300" DisplayMemberBinding="{Binding Title}"/>
                                        <GridViewColumn Header="Artist" Width="200" DisplayMemberBinding="{Binding Artist}"/>
                                        <GridViewColumn Header="Album" Width="200" DisplayMemberBinding="{Binding Album}"/>
                                        <GridViewColumn Header="Duration" Width="100" DisplayMemberBinding="{Binding Duration}"/>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </Grid>

                        <!-- Playlists View -->
                        <Grid x:Name="playlistsView" Visibility="Collapsed">
                            <StackPanel>
                                <TextBlock Text="Playlists" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>
                                <Button Content="Create New Playlist" Style="{StaticResource MaterialDesignRaisedButton}"
                                        HorizontalAlignment="Left" Margin="0,0,0,20"/>
                                <ListView x:Name="lstPlaylists" Background="Transparent" Foreground="White"/>
                            </StackPanel>
                        </Grid>

                        <!-- Recently Played View -->
                        <Grid x:Name="recentlyPlayedView" Visibility="Collapsed">
                            <StackPanel>
                                <TextBlock Text="Recently Played" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>
                                <ListView x:Name="lstRecentlyPlayed" Background="Transparent" Foreground="White"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
    </Border>
</Window>
